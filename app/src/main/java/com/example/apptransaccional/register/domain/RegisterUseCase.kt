package com.example.apptransaccional.register.domain

/*import com.example.apptransaccional.register.data.models.UserRequest
import com.example.apptransaccional.register.data.models.UserDTO
import com.example.apptransaccional.register.data.repository.RegisterRepository

class RegisterUseCase {
    private  val repository = RegisterRepository()

    suspend operator fun invoke(user: UserRequest) : Result<UserDTO> {
        val result = repository.createUser(user)

        return result
    }
}*/
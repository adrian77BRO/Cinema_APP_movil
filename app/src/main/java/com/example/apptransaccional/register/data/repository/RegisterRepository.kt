package com.example.apptransaccional.register.data.repository

//import com.example.apptransaccional.core.network.RetrofitHelper

class RegisterRepository()  {
    //private val registerService = RetrofitHelper.registerService

    /*suspend fun createUser(request : UserRequest) : Result<UserDTO> {
        return try {
            val response = registerService.createUser(request)
            if (response.isSuccessful) {
                Result.success(response.body()!!)
            } else {
                Result.failure(Exception(response.errorBody()?.string()))
            }
        } catch (e: Exception) {
            Result.failure(e)
        }
    }*/

    /*suspend fun register(username: String, email: String, password: String) =
        registerService.register(UserRequest(username, email, password))*/
}